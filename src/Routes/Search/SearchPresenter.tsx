import * as React from "react";
import styled from "../../typed-components";

const Container = styled.div`
  width: 100%;
`;

const SearchPresenter: React.SFC = ({}) => <Container />;
export default SearchPresenter;
